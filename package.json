{
  "name": "shelfielist",
  "version": "0.1.0",
  "private": true,
  "description": "Shared household inventory app for Shelfielist",
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning server.js",
    "dev": "node --watch --no-warnings=ExperimentalWarning server.js"
  },
  "dependencies": {
    "quagga": "^0.12.1"
  }
}
